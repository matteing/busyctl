[tools]
go = "1.26.5"

[tasks.build]
description = "Build busyctl"
run = "go build -o bin/busyctl ./cmd/busyctl"

[tasks.test]
description = "Run the test suite"
run = "go test ./..."

[tasks.check]
description = "Run tests and static analysis"
run = ["go test ./...", "go vet ./..."]
